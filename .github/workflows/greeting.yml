name: Greeting Workflow

on:
  push:
    branches:
      - nakamoto-test-actions

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello, world!"